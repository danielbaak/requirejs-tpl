define(['../tpl.js'], function(tpl) {
	describe('tpl', function() {
		beforeEach(function() {
			//TODO init
		});

		it('loads the template file', function() {
			expect(false).toBe(true);
		});

		it('compiles the template', function() {
			expect(false).toBe(true);
		});

		it('r.js precompiles the template ', function() {
			expect(false).toBe(true);
		});
	});	
});
